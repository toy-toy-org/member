spring:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/toytoy
    username: root
    password: root
mail:
  server:
    url: http://localhost:8080
